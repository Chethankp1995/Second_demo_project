job('maven_dsl_job_first') {

    description('Maven dsl project')

    scm {
        git('https://github.com/chethan710/Second_demo_project.git', 'master', {node -> node / 'extensions' << '' })
    }

    steps {
        maven {
            mavenInstallation('maven')
            goals('-B -DskipTests clean package')
        }
        maven {
            mavenInstallation('maven')
            goals('test')
        }
       
    }

    
}
